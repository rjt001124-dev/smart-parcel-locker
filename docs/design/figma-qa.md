# Figma 设计基线 QA

## 自动化验证

- [x] 插件单元测试覆盖目录、token、所有权、报告、依赖、布局、组件和页面内容。
- [x] TypeScript 严格类型检查通过。
- [x] esbuild 生成 `dist/code.js` 与 `dist/ui.html`。
- [x] Starter 免费套餐页面数量不超过 3。
- [x] 插件命名空间符合 Figma 字符限制。
- [x] 重复运行在 FakeFigmaPort 中保持资源数量不变。

## Figma 最终运行

- [ ] Foundations 状态为 success，错误和冲突均为 0。
- [ ] Components 状态为 success，错误和冲突均为 0。
- [ ] Mini App 状态为 success，错误和冲突均为 0。
- [ ] Admin Web 状态为 success，错误和冲突均为 0。
- [ ] 第二次运行四阶段仅更新资源，不增加页面或重复稳定 key。

## 00 Design System

- [ ] Foundations 显示设计原则、色板、排版、间距、圆角、阴影、图标和状态示例。
- [ ] Button、Input、Site Card、Data Table 等组件具有真实可见内容，不是名称占位框。
- [ ] 组件按两列评审网格排列，不重叠、不裁切。
- [ ] 文字、背景、边框和状态色对比清晰。

## 01 Mini App

- [ ] 13 个画板均为 375 × 812。
- [ ] 首页、城市、网点、柜格、订单确认、支付、存件、订单、补缴、取件和个人中心内容完整。
- [ ] 每个页面包含中文标题、业务数据、恢复操作和主按钮。
- [ ] 状态矩阵覆盖加载、空数据、网络失败、支付状态、柜格争抢、设备离线、开门失败、门未关、超时补缴和权限不足。

## 02 Admin Web

- [ ] 9 个核心画板均为 1440 × 900。
- [ ] 深色侧边导航与浅色内容区正确。
- [ ] Dashboard、网点、柜机、订单、支付、通知、权限和审计内容可辨识。
- [ ] 远程开门、重启、隔离和回滚包含二次确认、原因和审计提示。

## 批准

- [ ] 用户完成最终全局截图检查。
- [ ] `docs/design/figma-state-ledger.json` 的 `qa.approved` 更新为 `true`。
- [ ] 前端实现可以开始。
